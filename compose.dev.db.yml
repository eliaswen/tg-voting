services:
  postgres:
    image: postgres:18-alpine
    container_name: tg-voting-postgres
    environment:
      - POSTGRES_USER=tg-voting
      - POSTGRES_PASSWORD=wip
      - POSTGRES_DB=tg-voting
    ports:
      - "5432:5432"

volumes:
  pg-data: